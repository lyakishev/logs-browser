#FEAT: режим старт-стоп - интеллектуальный режим: посленажатия старт фиксиуется время всех щелчков мышью и после нажатия стоп ищутся логи, в таблице логов подсвечиваются логи: которые совпадают по времени щелчков - темным, далее по мере отдаления по времени (сколько секунд?) -цвет светлее (+СКРИНШОТЫ?)
#FEAT: щелчок - узел дерева, потомки - логи до(после?) этого щелчка до следующего
#FEAT: gtkmvc
#FEAT: ntplib
#FEAT: PL/SQL analyzer:
#	процедура, функция: тело, связи с другими объектами - через from и т1.п1=т2.п1 в where,
#	через другие функции, процедуры,
#	через cairo - диаграмму связей.
#FEAT: File Monitor - онлайн заполнение таблицы
#FEAT: возможность поставить логу метку в окне просмотра и через всплыв меню в таблице (метка слово+картинки, теги?)
#
####уменьшить вложенность дерева: если один потомок - объединять

####Ctrl-Z
####BUG: удаление пустых путей опять отвалилось
#
#create view current
#intersect
#
#шрифт подправить при раскрашивании таблицы

#опции:
#- кэшировать запросы или нет
#
#
#pygments - подсветка синтаксиса

####TODO: Горячие клавиши: expand all collapse all, collapse non-active, collapse non-active
#
#Конструктор запросов
#2. Combobox:from из Notebook
#5. NOT

PLSQLAnalyzer
сохранение запроса
select strftime('%H:%M:%S.%f',date), logname, type from $Page_1
where logname like 'Order%BL'
group by date, logname, type
order by date desc
---> log window ---> 
select date, logname, type, source, pretty(log), lid
                 from  where lid=25641 order by date asc, lid desc
from - пусто - окно не выводится

diff вкладок


Менеджер источников:
	конф файл - xml - прописано все
	поле выбора - стенды из xml
	полe выбора - фильтры - предустановленные (например фильтр, которые выделяет все кроме тех для которых не найдены форматы), что выбрать, отключить... + фильтры с других страниц
	тогда conf editor - не нужен
	индикация "возраста"
	определение формата логирования на этапе заполнения дерева (если формат не найден - подсвечивать) скидывать в словарь - передавать в process (mp_process) - чтобы тот не тратил время на одно и то же
	варианты перезагрузки дерева:
		только новые
		полное
		полное с обновлением форматов

	хранить информацию - пройденные строки, строка на котороый был найден формат - чтобы можно было посмотреть.

	режимы представления - flat, tree

Time out - сообщение : "Внимание! С момента последнего обновления дерева источников прошло ... Структура ФС на серверах могла имзениться. Рекомендуется обновить дерево источников" -> кнопки обновить и отмена.

даты
настройка отображать с время , дата/время

новый виджет для времени
entry + spin


отдельные функции - определение формата (чтобы можно было на этапе загрузки дерева это делать), определение начальной даты
Убрать опцию FTS из GUI
